#include <stdio.h>

int main() {
    int a;
    scanf("%d",&a);
    for(i = a ; i < n ; i++){
        for(j = i ; j <= i ; j++){
            printf("* ");
        }
        printf("\n");
    }
}